name: Update Dependencies

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  update-dependencies:
    uses: valkyrjaio/.github/.github/workflows/_update-php-dependencies.yml@2b626b4cd5090fb9d998ba35450775bfea79b21b
    secrets: inherit
    with:
      php-version: '8.4'
      dependencies: |
        [
          {"name": "Root Packages",  "command": "update",                  "directory": "."},
          {"name": "Rector",         "command": "rector-composer-update",   "directory": ".github/ci/rector"}
        ]
